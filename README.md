
  # Add Hover Animations

  This is a code bundle for Add Hover Animations. The original project is available at https://www.figma.com/design/hLPSoHMrlLnm2ULhGPu9ac/Add-Hover-Animations.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  